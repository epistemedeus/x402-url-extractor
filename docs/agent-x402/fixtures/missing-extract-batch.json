{
  "id": "missing-extract-batch",
  "expectReject": "tools/list missing or duplicate extract_batch",
  "kind": "inventory",
  "payload": {
    "tools": [
      {
        "name": "extract",
        "title": "Extract Web Page as Structured JSON",
        "inputSchema": {
          "type": "object",
          "additionalProperties": false,
          "required": ["url"],
          "properties": {
            "url": { "type": "string" }
          }
        }
      }
    ]
  }
}
