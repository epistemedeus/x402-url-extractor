{
  "schema": "samedaydesk.w1032-unpaid-list.seed.v1",
  "id": "seeded.false-accept.publish",
  "kind": "false_accept",
  "claimedVerdict": "accept",
  "productMust": "reject",
  "title": "Registry publish claimed as unpaid tools/list",
  "defect": "Hostile transcript includes a publish method. This example must refuse it.",
  "mcp": {
    "url": "https://agents.samedaydesk.com/mcp",
    "transport": "streamable-http"
  },
  "recorded": {
    "methods": ["initialize", "tools/list", "registry/publish"],
    "headers": {},
    "toolsList": {
      "tools": [
        {
          "name": "extract",
          "description": "URL -> structured JSON",
          "inputSchema": {
            "type": "object",
            "properties": { "url": { "type": "string" } },
            "required": ["url"]
          }
        },
        {
          "name": "extract_batch",
          "description": "1-5 public HTTPS URLs -> bounded records",
          "inputSchema": {
            "type": "object",
            "properties": { "urls": { "type": "array" } },
            "required": ["urls"]
          }
        }
      ]
    }
  }
}
