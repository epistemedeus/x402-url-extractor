{
  "schema": "samedaydesk.w1012-unpaid-list.seed.v1",
  "id": "seeded.false-accept.tools-call",
  "kind": "false_accept",
  "claimedVerdict": "accept",
  "productMust": "reject",
  "title": "Recorded tools/call claimed as unpaid tools/list",
  "defect": "Hostile transcript includes tools/call. This example must refuse it even if extract is listed.",
  "mcp": {
    "url": "https://agents.samedaydesk.com/mcp",
    "transport": "streamable-http"
  },
  "recorded": {
    "methods": ["initialize", "tools/list", "tools/call"],
    "headers": {},
    "toolsList": {
      "tools": [
        {
          "name": "extract",
          "description": "URL -> structured JSON",
          "inputSchema": {
            "type": "object",
            "properties": { "url": { "type": "string" } },
            "required": ["url"]
          }
        },
        {
          "name": "extract_batch",
          "description": "1-5 public HTTPS URLs -> bounded records",
          "inputSchema": {
            "type": "object",
            "properties": { "urls": { "type": "array" } },
            "required": ["urls"]
          }
        }
      ]
    }
  }
}
