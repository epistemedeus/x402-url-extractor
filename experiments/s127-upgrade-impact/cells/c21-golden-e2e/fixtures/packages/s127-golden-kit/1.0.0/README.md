# s127-golden-kit@1.0.0 (synthetic)

Old tree for the c21 golden end-to-end packet. Label: `synthetic`. Not live-capture. Not a published package. No paid demand.

Main surface (`.`) exports: `format`, `usedRemoved`, `unusedRemoved`, `keep`.

Plugin surface (`./plugin`) exports: `dynamicRemoved`, `pluginKeep`.
