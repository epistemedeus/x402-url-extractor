/**
 * Synthetic s127-golden-kit/plugin@2.0.0 (new). Label: synthetic. Not published.
 * Removed: dynamicRemoved. Caller loads this specifier only via import();
 * PACKET-CONTRACT rule 4: dynamic import ⇒ unknown for that surface.
 */

export function pluginKeep(value) {
  return value;
}
