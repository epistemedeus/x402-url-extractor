# s127-golden-kit@2.0.0 (synthetic)

New tree for the c21 golden end-to-end packet. Label: `synthetic`. Not live-capture. Not a published package. No paid demand.

Main surface (`.`) exports: `format`, `keep`. Removed: `usedRemoved`, `unusedRemoved`.

Plugin surface (`./plugin`) exports: `pluginKeep`. Removed: `dynamicRemoved`.
