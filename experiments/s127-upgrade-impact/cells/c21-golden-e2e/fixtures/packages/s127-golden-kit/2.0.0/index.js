/**
 * Synthetic s127-golden-kit@2.0.0 main surface (new). Label: synthetic. Not published.
 * Removed: usedRemoved (statically imported by the caller) and unusedRemoved (not imported).
 * A newer version is not itself a break. Unused export change is not a caller defect.
 */

export function format(value) {
  return String(value);
}

export function keep(value) {
  return value;
}
