Lifecycle `preinstall` / `install` / `postinstall` strings in
`s127-demo-lib.packument.json` are metadata only.

This directory is not an installable package. Do not `npm install`.
Do not run those commands. `SCRIPT_RAN.marker` must stay absent.
