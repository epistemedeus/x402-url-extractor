# Drop-in: real-source case B

Label: `fixture` once filled. Empty on purpose.

Place two decision JSONs here when c12-real-b (or the integrator) has them:

- `method-a.json` — registry version + changelog skim decision
- `method-b.json` — usage-binding packet

Missing files keep the real-ab suite at `unknown`. Do not invent a kill. This directory is owned by c22; do not write into `fixtures/real-b/` or `cells/c12-real-b/` from this cell.
