# Evidence — real-source case A

Live capture `2026-09-10T10:48:00Z` of official npm `path-to-regexp`
`6.3.0` and `8.4.2`. Replay bytes live under `fixtures/real-a/`
(`PROVENANCE.json`).

- `CAPTURED_AT_UTC.txt` — frozen clock
- `sha256sums.txt` — tarball, version-doc, slim packument, extracted JS
- `official-quotes.md` — load-bearing lines from published `dist/index.js`
- `s122-contrast.md` — why this is not `review_changelog`
- `capture-receipt.json` — GET + tar-only, no lifecycle scripts
