{
  "name": "s117-requester-delivery",
  "version": "0.1.0",
  "private": true,
  "description": "Offline requester residuals: x402 V2 settlement-txid recovery, routeTemplate decode drift, paid-call timeout/retry safety, hung well-known abort, unreadable spend-limit binding, and CDP validate-vs-index classification. Does not pay, sign, or open external PRs.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "s117-requester-delivery": "./bin/cli.mjs"
  },
  "scripts": {
    "start": "node bin/cli.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "license": "MIT"
}
