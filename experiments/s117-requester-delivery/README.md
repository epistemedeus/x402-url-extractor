# S117 requester delivery

Offline residuals for currently open external requester jobs. This tree does not change live SameDayDesk payment routes, does not pay, sign, or broadcast, and does not open external PRs or comments.

```bash
export PATH="/home/ubuntu/.nvm/versions/node/v22.23.2/bin:$PATH"
cd experiments/s117-requester-delivery
npm test
node bin/cli.mjs settlement --fixture fixtures/settlement/aibtc666-provided-report.json
```

A successful local recipe is not fulfillment of the upstream request. Free diagnostics stay distinct from any later paid follow-on. Root owns contact.

See `PACKETS.md` for ranked requester packets and `RESULT.md` for commit/test provenance.
