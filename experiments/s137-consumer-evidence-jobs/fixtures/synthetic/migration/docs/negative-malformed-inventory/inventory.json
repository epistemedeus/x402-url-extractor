{
  "schema": "s137.migration.inventory.v1",
  "caller": "synthetic-malformed",
  "notes": "operations must be an array of {method,route}. This object is the negative.",
  "operations": {
    "extract": "GET /extract"
  }
}
